auto-install-peers = true
# Enable building from source for native dependencies
enable-pre-post-scripts=true
# Ensure native dependencies are built in production
ignore-scripts=false
# Use hoisted node_modules structure for better compatibility
shamefully-hoist=true
# Ensure all platforms can access the binaries
unsafe-perm=true
